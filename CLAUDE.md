# KUDU Product UI working instructions

Read `AGENTS.md`, then `.agents/skills/kudu-product-ui/SKILL.md` before KUDU UI work. The referenced skill is the canonical design contract in this repository.

For native Claude Code skill discovery in another project, use the installer with `--tool claude`; it copies the complete self-contained skill to `.claude/skills/kudu-product-ui/`. This instruction file does not install a skill into other repositories or into a web account.

Keep the approved product direction and all 45 decisions intact. Distinguish source brand facts, user-approved choices, draft token values, and untested proposals. Preserve existing behavior during UI migration. Do not invent fonts, logos, business metrics, or successful test results.
